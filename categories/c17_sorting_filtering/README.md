# c17 — Sorting and filtering text

`sort` (plain, `-r`, `-n`, `-u`), `grep` (plain, `-v`, `^` anchor), `awk`
length filtering, sorting by a computed key.

Source: [w3resource: Sorting and filtering text](https://www.w3resource.com/bash-script-exercises/sorting-and-filterting-text.php)
(category 3 of 6: *Text Processing* — the last one in this group, and
this subcategory has 9 exercises, not 10).

See [`EXERCISES.md`](EXERCISES.md) — ids `c17-01` through `c17-09`.
