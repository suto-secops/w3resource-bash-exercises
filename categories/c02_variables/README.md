# c02 — Variable declaration and usage

Variable declaration, interpolation, reassignment, arrays, `local` scope,
command substitution into a variable, special parameters (`$#`, `$@`,
`$0`).

Source: [w3resource: Variable declaration and usage](https://www.w3resource.com/bash-script-exercises/variable-declaration-and-usage.php)
(category 1 of 6: *Basic Bash Syntax and Usage*).

No starting files needed for any exercise here. Usual flow:
`bin/ex next` / `bin/ex check <id>` from the repo root, inside the
container.

See [`EXERCISES.md`](EXERCISES.md) — ids `c02-01` through `c02-10`.
