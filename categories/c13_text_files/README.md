# c13 — Manipulating text files

`wc` (`-l`/`-w`/`-m`), `grep`, `sed -i`, `sort`, `awk`, `cat`, in-place
editing via a temp file + `mv`.

Source: [w3resource: Manipulating text files](https://www.w3resource.com/bash-script-exercises/manipulating-text-files.php)
(category 3 of 6: *Text Processing*).

See [`EXERCISES.md`](EXERCISES.md) — ids `c13-01` through `c13-10`.
